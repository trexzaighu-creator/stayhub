# Frontend — Next.js + TypeScript + Tailwind

This folder contains a minimal Next.js frontend scaffold designed to integrate with the Django backend API.

Quick start:

```powershell
cd frontend_next
npm install
npm run dev
```

The app expects the backend API at `http://localhost:8000/api` by default; configure environment variables as needed.
